apiVersion: v1
kind: Service
metadata:
  name: {{ include "flask-cicd.fullname" . }}
  labels:
    {{- include "flask-cicd.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "flask-cicd.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
