apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "flask-cicd.fullname" . }}-config
  labels:
    {{- include "flask-cicd.labels" . | nindent 4 }}
data:
{{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
{{- end }}
